import { css } from 'lit'

export const sharedStyles = css`
  /* clear default button style */
  button {
    border: none;
    background: none;
    color: inherit;
    outline: inherit;
    cursor: pointer;
  }
  
  svg {
    outline: none;
  }

  h1 {
    font-size: 2.1rem;
  }
`
